---
- name: Converge
  hosts: all
  tasks:
    - name: Install requirements
      apt:
        name: gnupg
        state: present
        update_cache: true
    - name: "Include leadlineit.postgresql"
      include_role:
        name: "leadlineit.postgresql"
